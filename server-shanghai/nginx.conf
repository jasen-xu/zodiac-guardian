# 易道管理后台 Nginx 配置
# 放置位置: /etc/nginx/sites-available/yidao-admin
# 然后: sudo ln -s /etc/nginx/sites-available/yidao-admin /etc/nginx/sites-enabled/

server {
    listen 80;
    server_name admin.yi-yao.net;

    # 重定向到 HTTPS（SSL 配置好后启用）
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 30s;
    }
}

# HTTPS 配置（申请 SSL 证书后启用）
# server {
#     listen 443 ssl http2;
#     server_name admin.yi-yao.net;
#
#     ssl_certificate /etc/letsencrypt/live/admin.yi-yao.net/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/admin.yi-yao.net/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:3001;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
